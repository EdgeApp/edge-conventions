# fixcode

Implement fixes from a plan document by editing code, leaving changes unstaged for review.

## Instructions

Follow the `@.cursor/skills/fix-code/SKILL.md` skill to execute this command.

The user must provide a plan document path containing the fixes to implement.
